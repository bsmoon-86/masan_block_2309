<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>
        UPDATE user Page
    </h1>

    <form action="" method="">
        <label>ID</label>
        <!-- 서버가 유저에게 보내는 아이디 값을 대입, 해당하는 입력 공간은 수정이 불가능 -->
        <input type="text" name="_id" value="<%=data.id%>" readonly><br>
        <!-- password는 수정 가능 -->
        <label>Password</label>
        <input type="password" name="_pass" value="<%=data.password%>"><br>
        <!-- name은 수정 불가능 -->
        <label>Name</label>
        <input type="text" name="_name" value = "<%=data.name%>"><br>
        <!-- Tel은 수정 가능 -->
        <label>Tel</label>
        <input type="text" name="_tel" value = "<%=data.tel%>"><br>
        <!-- Loc는 수정 가능 -->
        <label>Loc</label>
        <input type="text" name="_loc" value = "<%=data.loc%>"><br>
        <!-- Gender는 수정 불가능 -->
        <label>Gender</label>
        <!-- gender 값은 0이나 1 
            값이 0인 경우에는 첫번째 라디오 버튼이 선택
            값이 1이라면 두번째 라디오 버튼이 선택
        -->
        <% if (data.gender == 0) { %>
            <input type="radio" name="_gender" value="0" checked>남자
            <input type="radio" name="_gender" value="1">여자<br>
        <% } else { %>
            <input type="radio" name="_gender" value="0">남자
            <input type="radio" name="_gender" value="1" checked>여자<br>
        <% } %>
        <!-- Email은 수정 가능 -->
        <label>Email</label>
        <input type="email" name="_email" value="<%=data.email%>"><br>
        <input type="submit">
    </form>
    <!-- 
        서버에서 유저에게 {data : 회원정보}  
        회원정보의 데이터의 형태 -> 
        [id : 아이디값, password : 비밀번호, name : 이름, loc : 지역, gender : 성별, ..]
    -->
</body>
</html>