<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script>
        function update_content(_no){
            location.href = '/board/update_content/'+_no
            // 글번호 1번 인 경우 
            // /board/update_content/1
        }
        function board(){
            location.href = '/board'
        }
        function delete_content(_no){
            location.href = '/board/delete_content/'+_no
        }
    </script>
</head>
<body>
    <!-- 글 목록으로 되돌아가는 버튼 -->
    <button onclick="board()">글 목록</button>
    <!-- 작성자와 로그인을 한 아이디 값이 같은 경우 글 수정 버튼을 보여준다. -->
    <% if(data.writer == id){ %>
        <button onclick="update_content('<%=no%>')">글 수정</button>
        <button onclick="delete_content('<%=no%>')">글 삭제</button>
    <% } %>
    <table>
        <tr>
            <th>
                글 제목
            </th>
            <td>
                <%=data.title%>
            </td>
        </tr>
        <tr>
            <th>작성자</th>
            <td>
                <%=data.writer%>
            </td>
        </tr>
        <tr>
            <th>작성 시간</th>
            <td><%=data.time%></td>
        </tr>
        <tr>
            <th>본문</th>
            <td><%=data.content%></td>
        </tr>

    </table>
</body>
</html>