<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <%- include('./refer/head.ejs') %>
</head>
<body>
    <%- include('./refer/menu.ejs') %>
    <div class="container">
        <h1>설문조사</h1>
        <div class="container">
            <!-- 
                q -> [q1, q2, q3]
                a -> [a1, a2, a3]
                a -> [
                    [a1-1, a1-2, a1-3], 
                    [a2-1, a2-2, a2-3], 
                    [a3-1, a3-2, a3-3]
                ]
            -->
            <form action="/survey/submit" method="post">
                <div style="display: none;">
                    <input type="number" name="no" value="<%=no%>" readonly>
                </div>
                <div class="mb-3">
                    <%for (let i = 0; i < q.length;i++){%>
                        <label class="form-label" ><%=q[i]%></label>
                        <%for(let j =0; j < a[i].length; j++){%>
                            <div class="form-check">
                                <input type="radio" class="form-check-input" 
                                name="a<%=i+1%>" id="a<%=i+1%>-<%=j%>" value="<%=j%>">
                                <label class="form-check-label" for="a<%=i+1%>-<%=j%>">
                                    <%=a[i][j]%>
                                </label>
                            </div>
                        <%}%>
                    <%}%>
                    <input type="submit" value="제출" class="btn">
                </div>
            </form>
        </div>
    </div>
    
</body>
</html>